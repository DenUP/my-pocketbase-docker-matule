# PocketBase + Swagger UI стек для мобильной разработки (Профессионалы 2026)

Данный репозиторий содержит готовую Docker-сборку для быстрого развёртывания серверной части мобильного приложения, разработанного в рамках компетенции **«Мобильная разработка» Всероссийского чемпионатного движения по профессиональному мастерству «Профессионалы» 2026**.

**Макет в Figma:** [https://www.figma.com/design/pbwY6r5TtWUkxMsoJBWf0c/Matule-2026?node-id=1-2&p=f&t=E5lU1y1Emy3WN5ZN-0](https://www.figma.com/design/pbwY6r5TtWUkxMsoJBWf0c/Matule-2026?node-id=1-2&p=f&t=E5lU1y1Emy3WN5ZN-0)

Стек включает:
- **PocketBase** — серверная часть (база данных, аутентификация, API).
- **Swagger UI** — интерактивная документация API на основе OpenAPI-спецификации.

Все данные PocketBase (база SQLite, загруженные файлы) автоматически сохраняются на вашем компьютере и не теряются после остановки контейнеров.

---

## 🚀 Быстрый старт

### 1. Что нужно установить
- **Docker Desktop** для Windows (скачать с [официального сайта](https://www.docker.com/products/docker-desktop/)) — обязательно запустите его после установки.
- **Git** (скачать с [git-scm.com](https://git-scm.com/)) — для клонирования репозитория.
- Терминал (подойдёт PowerShell или командная строка).

### 2. Скопируйте файлы на свой компьютер
Если вы получили этот проект как репозиторий, склонируйте его:
```bash
git clone <ссылка-на-ваш-репозиторий>
cd <название-папки>
```
Если вы скачали архив, распакуйте его в удобную папку (например, C:\MyProject).

Внутри вы должны увидеть такую структуру:

```bash
├── docker-compose.yml      # главный файл для запуска всех сервисов
├── swagger/                 # папка со Swagger UI
│   ├── Dockerfile           # инструкция для сборки образа Swagger UI
│   └── api.yaml             # OpenAPI спецификация вашего API
├── pocketbase_data/          # папка для данных PocketBase (создаётся автоматически)
└── README.md                # этот файл
```

### 3. Запустите стек
Откройте терминал в папке, где лежит docker-compose.yml, и выполните команду:
```bash
docker-compose up -d
```

Введите в браузере следующие адреса:

-  PocketBase Admin: http://localhost:8090/_/
  — здесь вы будете управлять базой данных и пользователями.

- Swagger UI: http://localhost:8080
— здесь вы увидите документацию API и сможете отправлять тестовые запросы.

### 4. Как создать администратора (первый вход)?

При первом запуске PocketBase в логах контейнера появится специальная одноразовая ссылка для создания суперпользователя. Чтобы увидеть её:

```bash
docker logs pocketbase
```

Найдите строку, начинающуюся с ```http://0.0.0.0:80/_/#/pbinstal/...```, скопируйте её и вставьте в браузер, заменив ```0.0.0.0:80``` на ```localhost:8090```

### 4. Импортировать БД?

Прикрепил файл ```pb_schema.json``` через Админку в PocketBase